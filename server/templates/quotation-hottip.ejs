<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>HOTTIP INDIA POLYMERS QUOTATION</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: "Arial", "Helvetica", sans-serif;
        font-size: 11px;
        line-height: 1.3;
        color: #000;
        padding: 15px;
        background: #fff;
      }
      .header {
        text-align: center;
        margin-bottom: 15px;
        border: 2px solid #000;
        padding: 12px 10px;
        background: linear-gradient(to bottom, #f8f9fa 0%, #ffffff 100%);
      }
      .header h1 {
        font-size: 20px;
        font-weight: bold;
        margin-bottom: 3px;
        color: #003366;
        letter-spacing: 1px;
      }
      .header .subtitle {
        font-size: 13px;
        font-weight: bold;
        margin-bottom: 8px;
        color: #003366;
      }
      .header .address {
        font-size: 10px;
        line-height: 1.4;
        margin-bottom: 5px;
      }
      .header .contact {
        font-size: 10px;
        margin-bottom: 3px;
      }
      .header .gstin {
        font-size: 10px;
        font-weight: bold;
        margin-top: 3px;
      }
      .info-section {
        display: flex;
        justify-content: space-between;
        margin-bottom: 15px;
        gap: 20px;
      }
      .customer-block {
        flex: 1;
        border: 1px solid #000;
        padding: 10px;
        background: #f9f9f9;
      }
      .customer-block .title {
        font-weight: bold;
        font-size: 11px;
        margin-bottom: 8px;
        text-decoration: underline;
      }
      .customer-block p {
        margin: 3px 0;
        font-size: 10px;
        line-height: 1.4;
      }
      .quotation-details {
        flex: 1;
        border: 1px solid #000;
        padding: 10px;
        background: #f9f9f9;
      }
      .quotation-details p {
        margin: 4px 0;
        font-size: 10px;
      }
      .quotation-details strong {
        display: inline-block;
        width: 130px;
        font-weight: bold;
      }
      .part-section {
        margin-bottom: 20px;
        page-break-inside: avoid;
      }
      .part-header {
        font-weight: bold;
        font-size: 12px;
        margin-bottom: 0;
        padding: 6px 10px;
        background: #ffff00;
        border: 1px solid #000;
        border-bottom: 2px solid #000;
      }
      .quotation-for-title {
        font-weight: bold;
        font-size: 11px;
        margin-top: 12px;
        margin-bottom: 8px;
        padding: 5px 8px;
        background: #fff;
        border: none;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 12px;
        font-size: 10px;
      }
      table.mold-table th {
        border: 1px solid #000;
        padding: 4px 3px;
        text-align: center;
        vertical-align: middle;
        background: #ffff00;
        font-weight: bold;
        font-size: 8px;
        line-height: 1.2;
      }
      table.mold-table td {
        border: 1px solid #000;
        padding: 5px 3px;
        text-align: center;
        vertical-align: middle;
        font-size: 9px;
      }
      table.items-table th {
        border: 1px solid #000;
        padding: 6px 8px;
        text-align: center;
        background: #d9d9d9;
        font-weight: bold;
        font-size: 10px;
      }
      table.items-table td {
        border: 1px solid #000;
        padding: 5px 8px;
        vertical-align: middle;
        font-size: 10px;
      }
      table.items-table td.no {
        text-align: center;
        width: 5%;
      }
      table.items-table td.name {
        text-align: left;
        width: 40%;
      }
      table.items-table td.qty {
        text-align: center;
        width: 10%;
      }
      table.items-table td.price {
        text-align: right;
        width: 20%;
      }
      table.items-table td.amount {
        text-align: right;
        width: 20%;
      }
      table.items-table tr.total-row {
        background: #e8e8e8;
        font-weight: bold;
      }
      table.items-table tr.total-row td {
        border-top: 2px solid #000;
      }
      .summary-section {
        margin-top: 20px;
        margin-bottom: 20px;
        border: 1px solid #000;
        padding: 10px 15px;
        background: #f9f9f9;
      }
      .summary-row {
        display: flex;
        justify-content: space-between;
        padding: 5px 0;
        font-size: 11px;
      }
      .summary-row.grand-total {
        border-top: 2px solid #000;
        margin-top: 8px;
        padding-top: 8px;
        font-weight: bold;
        font-size: 13px;
      }
      .footer {
        margin-top: 20px;
        font-size: 10px;
        line-height: 1.5;
        border-top: 2px solid #000;
        padding-top: 12px;
      }
      .footer p {
        margin: 5px 0;
      }
      .footer strong {
        font-weight: bold;
      }
      .text-right {
        text-align: right;
      }
      .text-center {
        text-align: center;
      }
      @media print {
        body {
          padding: 10px;
        }
        .page-break {
          page-break-before: always;
        }
      }
    </style>
  </head>
  <body>
    <!-- HEADER -->
    <div class="header">
      <h1>HOTTIP INDIA POLYMERS</h1>
      <div class="subtitle">HOT RUNNER SYSTEMS</div>
      <div class="address">
        Gat no 209, Office No 406, Swaraj Capital Borhadewadi<br />
        Chikhali Moshi Road, Pune 412105, Maharashtra, India
      </div>
      <div class="contact">
        www.hottipindia.com | sales@hottipindia.com | Mob: 9028018877
      </div>
      <div class="gstin">
        GSTIN: <%= quotation.companyGstin || '27AQYPM1029M1Z6' %>
      </div>
    </div>

    <!-- CUSTOMER & QUOTATION INFO -->
    <div class="info-section">
      <div class="customer-block">
        <div class="title">CUSTOMER ADDRESS</div>
        <p>
          <strong><%= quotation.customerName || customer?.name || 'N/A' %></strong>
        </p>
        <p><%= quotation.customerAddress || customer?.address || 'N/A' %></p>
        <p>
          GST.NO – <%= quotation.customerGstNo || customer?.gstNumber || 'N/A' %>
        </p>
        <p>Ph: <%= quotation.customerPhone || customer?.phone || 'N/A' %></p>
        <p>Mail: <%= quotation.customerEmail || customer?.email || 'N/A' %></p>
        <p>
          Kind Attn: <%= quotation.customerContactPerson || customer?.contactPerson || 'N/A' %>
        </p>
      </div>

      <div class="quotation-details">
        <p>
          <strong>Quotation Ref No :</strong> <%= quotation.quotationNumber %>
        </p>
        <p>
          <strong>Quotation Date :</strong> <%= quotation.quotationDate ? new Date(quotation.quotationDate).toLocaleDateString("en-IN") : 'N/A' %>
        </p>
        <p>
          <strong>Project Incharge :</strong> <%= quotation.projectIncharge || '-' %>
        </p>
        <p>
          <strong>Job Card Details :</strong> <%= quotation.jobCardNumber || '-' %>
        </p>
      </div>
    </div>

    <!-- MOLD / PART DETAILS SECTIONS -->
    <% if (quotation.moldDetails && quotation.moldDetails.length > 0) { %>
    <% quotation.moldDetails.forEach(function(mold, moldIndex){ %>
    <div class="part-section">
      <div class="part-header"><%= moldIndex+1 %>) MOLD / PART DETAILS</div>

      <!-- Mold Details Table (13 fields in 4 rows) -->
      <table class="mold-table">
        <tbody>
          <tr>
            <td style="width: 5%; font-weight: bold">1</td>
            <td style="width: 15%; text-align: left; font-weight: bold">
              Part Name
            </td>
            <td style="width: 20%; text-align: left">
              <%= mold.partName || '-' %>
            </td>
            <td style="width: 5%; font-weight: bold">5</td>
            <td style="width: 15%; text-align: left; font-weight: bold">
              Plastic Material
            </td>
            <td style="width: 20%; text-align: left">
              <%= mold.plasticMaterial || '-' %>
            </td>
            <td style="width: 5%; font-weight: bold">9</td>
            <td style="width: 15%; text-align: left; font-weight: bold">
              Colour Change
            </td>
            <td style="width: 20%; text-align: left">
              <%= mold.colourChange || '-' %>
            </td>
          </tr>
          <tr>
            <td style="font-weight: bold">2</td>
            <td style="text-align: left; font-weight: bold">Mould no</td>
            <td style="text-align: left"><%= mold.mouldNo || '-' %></td>
            <td style="font-weight: bold">6</td>
            <td style="text-align: left; font-weight: bold">MFI</td>
            <td style="text-align: left"><%= mold.mfi || '-' %></td>
            <td style="font-weight: bold">10</td>
            <td style="text-align: left; font-weight: bold">Wall Thickness</td>
            <td style="text-align: left"><%= mold.wallThickness || '-' %></td>
          </tr>
          <tr>
            <td style="font-weight: bold">3</td>
            <td style="text-align: left; font-weight: bold">No of Cavity</td>
            <td style="text-align: left"><%= mold.noOfCavity || '-' %></td>
            <td style="font-weight: bold">7</td>
            <td style="text-align: left; font-weight: bold">GF% + MP%</td>
            <td style="text-align: left">
              <%= mold.gfPercent || '0' %>% + <%= mold.mfPercent || '0' %>%
            </td>
            <td style="font-weight: bold">11</td>
            <td style="text-align: left; font-weight: bold">
              System Suggested
            </td>
            <td style="text-align: left"><%= mold.systemSuggested || '-' %></td>
          </tr>
          <tr>
            <td style="font-weight: bold">4</td>
            <td style="text-align: left; font-weight: bold">No of Drops</td>
            <td style="text-align: left"><%= mold.noOfDrops || '-' %></td>
            <td style="font-weight: bold">8</td>
            <td style="text-align: left; font-weight: bold">
              Part Weight (Gms)
            </td>
            <td style="text-align: left"><%= mold.partWeight || '-' %> GMS</td>
            <td style="font-weight: bold">12</td>
            <td style="text-align: left; font-weight: bold">Trial Date</td>
            <td style="text-align: left">
              <%= mold.trialDate ? new Date(mold.trialDate).toLocaleDateString('en-IN') : '-' %>
            </td>
          </tr>
        </tbody>
      </table>

      <!-- Quotation For Title -->
      <div class="quotation-for-title">Quotation For <%= mold.partName || 'Part ' + (moldIndex+1) %> <%= mold.quotationFor ? '- ' + mold.quotationFor : '' %></div>

      <!-- Quotation Items Table -->
      <% var moldItems = quotation.quotationItems ? quotation.quotationItems.filter(function(item) { return item.partName === mold.partName || item.no === mold.no; }) : []; %>
      <% if (moldItems.length > 0) { %>
      <table class="items-table">
        <thead>
          <tr>
            <th style="width: 5%">No</th>
            <th style="width: 25%">Part Name</th>
            <th style="width: 30%">Part Description</th>
            <th style="width: 10%">QTY</th>
            <th style="width: 10%">UOM</th>
            <th style="width: 10%">Unit Price (INR)</th>
            <th style="width: 10%">Amount (INR)</th>
          </tr>
        </thead>
        <tbody>
          <% var moldTotal = 0; %>
          <% moldItems.forEach(function(item, itemIndex){ %>
            <% moldTotal += item.amount || 0; %>
          <tr>
            <td class="no"><%= itemIndex+1 %></td>
            <td class="name"><%= item.partName || '-' %></td>
            <td class="name"><%= item.partDescription || '-' %></td>
            <td class="qty"><%= item.qty || 0 %></td>
            <td class="text-center"><%= item.uom || 'NOS' %></td>
            <td class="price"><%= (parseFloat(item.unitPrice) || 0).toLocaleString('en-IN', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %></td>
            <td class="amount"><%= (parseFloat(item.amount) || 0).toLocaleString('en-IN', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %></td>
          </tr>
          <% }); %>
          <tr class="total-row">
            <td colspan="6" class="text-right"><strong>TOTAL INR</strong></td>
            <td class="amount"><strong><%= moldTotal.toLocaleString('en-IN', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %></strong></td>
          </tr>
        </tbody>
      </table>
      <% } %>
    </div>
    <% }); %><% } %>

    <!-- FINANCIAL SUMMARY -->
    <div class="summary-section">
      <div class="summary-row">
        <span><strong>Basic Amount</strong></span>
        <span><strong><%= (quotation.subtotalAmount || basicAmount || 0).toLocaleString('en-IN', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %></strong></span>
      </div>
      <div class="summary-row">
        <span><strong>IGST <%= quotation.gstPercentage || 18 %>%</strong></span>
        <span><strong><%= (gst || 0).toLocaleString('en-IN', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %></strong></span>
      </div>
      <div class="summary-row grand-total">
        <span>GRAND TOTAL</span>
        <span><%= (grandTotal || 0).toLocaleString('en-IN', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %></span>
      </div>
    </div>

    <!-- FOOTER: TERMS & CONDITIONS -->
    <div class="footer">
      <p>
        <strong>Delivery Period :</strong> <%= quotation.deliveryTerms || '25 to 30 Days from the Date of Approval Drawings' %>
      </p>

      <p>
        <strong>Packaging :</strong> <%= quotation.packaging || 'Standard Packaging' %>
      </p>

      <p><strong>Notes:</strong></p>
      <p style="margin-left: 15px">
        A) One Nozzle Heater and one Nozzle T/C are included in the package.
      </p>
      <p style="margin-left: 15px">
        B) Standard warranty 18 months from supply date.
      </p>

      <p style="margin-top: 10px">
        <strong>Bank Details :</strong>
        <%= quotation.bankName || 'Hottip India Polymers' %> | A/C No <%= quotation.bankAccountNo || '738305000994' %> | Branch <%= quotation.bankBranch || 'Moshi' %> | IFSC <%= quotation.bankIfscCode || 'ICIC0007383' %>
      </p>

      <p>
        <strong>Payment :</strong> <%= quotation.paymentTerms || '50 % Advance & 50 % Against Delivery' %>
      </p>
    </div>
  </body>
</html>

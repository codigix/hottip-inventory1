import React from "react";

const GlobalAuditLog: React.FC = () => (
  <div>
    <h1>Global Audit Log</h1>
    {/* Add audit log table, filters, and export options here */}
  </div>
);

export default GlobalAuditLog;
